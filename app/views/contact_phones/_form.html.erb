<%= form_with(model: contact_phone) do |form| %>
  <% if contact_phone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contact_phone.errors.count, "error") %> prohibited this contact_phone from being saved:</h2>
      <ul>
        <% contact_phone.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
<div class="content-field">
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone %>
  </div>

  <div class="btn btn-outline-info">
    <%= form.submit :Create %>
  </div>
</div>
<% end %>
